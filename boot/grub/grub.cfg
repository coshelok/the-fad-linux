search --no-floppy --file --set=root /boot_staging/boot/grub/grub.cfg
set default=0
set timeout=5

menuentry "FAD Linux" {
    linux /boot_staging/boot/kernel console=tty1 loglevel=7 init=/init
    initrd /boot_staging/boot/initramfs.cpio.gz
}
